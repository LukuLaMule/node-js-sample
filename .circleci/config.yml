version: 2.1

orbs:
  docker: circleci/docker@2.8.2


workflows:
  build-docker-image:
    jobs:
      - docker/publish:
          docker-password: DOCKER_PASSWORD
          docker-username: DOCKER_USERNAME
          image: $DOCKERHUB_USERNAME/nodejs-image-demo
         